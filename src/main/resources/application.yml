server:
  port: 8080

spring:
  profiles:
    active: local
  datasource:
    driver-class-name:  com.mysql.cj.jdbc.Driver
    url: ${JDBC_URL}
    username:  ${JDBC_USERNAME}
    password: ${JDBC_PASSWORD}
